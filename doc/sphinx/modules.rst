Modules
========

Dissemin is organized in Python modules implementing more or less independent features.
The modules :doc:`papers`, :doc:`publishers` and :doc:`statistics` are Django apps implementing
the web platform itself. The module :doc:`backend` implements the data ingestion processes.

.. toctree::
   :maxdepth: 4

   backend
   dissemin
   import_researchers
   manage
   papers
   publishers
   scratch
   statistics
   sword
